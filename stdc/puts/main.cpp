#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main(int argc, char **argv)
{
    printf("hello, world!\n");
    puts("hello, world!"); // same
    fprintf(stdout, "hello, world!\n"); // same
    return 0;
}
